export class Bezier {
  // для 3х точек
  // let x = Math.pow(1 - time, 2) * (w / 2) + 2 * (1 - time) * time * (w / 2 - 100) + Math.pow(time, 2) * (w / 2 + 150);
  // let y = Math.pow(1 - time, 2) * (h / 2) + 2 * (1 - time) * time * (h / 2 - 200) + Math.pow(time, 2) * (h / 2 + 150);

  // P = (1−t)3P1 + 3(1−t)2tP2 +3(1−t)t2P3 + t3P4

}
